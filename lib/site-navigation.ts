export {
  defaultNavigationContent,
  type NavItem,
  type NavigationContent,
} from "@/lib/navigation-defaults"

import { defaultNavigationContent } from "@/lib/navigation-defaults"

/** @deprecated Use `getNavigationContent()` or `useNavigation()` instead. */
export const mainNavItems = defaultNavigationContent.headerNavItems
